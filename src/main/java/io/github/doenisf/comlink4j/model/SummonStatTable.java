/*
 * swgoh-comlink
 * An API for sending requests to Star Wars Galaxy of Heroes' game services
 *
 * The version of the OpenAPI document: 0.34.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.github.doenisf.comlink4j.model;

import com.google.gson.*;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.doenisf.JSON;

import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/**
 * SummonStatTable
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-04T16:29:17.254694600+02:00[Europe/Berlin]", comments = "Generator version: 7.8.0")
public class SummonStatTable {
  public static final String SERIALIZED_NAME_RARITY = "rarity";
  @SerializedName(SERIALIZED_NAME_RARITY)
  private Rarity rarity;

  public static final String SERIALIZED_NAME_STAT_TABLE = "statTable";
  @SerializedName(SERIALIZED_NAME_STAT_TABLE)
  private String statTable;

  public SummonStatTable() {
  }

  public SummonStatTable rarity(Rarity rarity) {
    this.rarity = rarity;
    return this;
  }

  /**
   * Get rarity
   * @return rarity
   */
  @javax.annotation.Nullable
  public Rarity getRarity() {
    return rarity;
  }

  public void setRarity(Rarity rarity) {
    this.rarity = rarity;
  }


  public SummonStatTable statTable(String statTable) {
    this.statTable = statTable;
    return this;
  }

  /**
   * Get statTable
   * @return statTable
   */
  @javax.annotation.Nullable
  public String getStatTable() {
    return statTable;
  }

  public void setStatTable(String statTable) {
    this.statTable = statTable;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SummonStatTable summonStatTable = (SummonStatTable) o;
    return Objects.equals(this.rarity, summonStatTable.rarity) &&
        Objects.equals(this.statTable, summonStatTable.statTable);
  }

  @Override
  public int hashCode() {
    return Objects.hash(rarity, statTable);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SummonStatTable {\n");
    sb.append("    rarity: ").append(toIndentedString(rarity)).append("\n");
    sb.append("    statTable: ").append(toIndentedString(statTable)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("rarity");
    openapiFields.add("statTable");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SummonStatTable
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SummonStatTable.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SummonStatTable is not found in the empty JSON string", SummonStatTable.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!SummonStatTable.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `SummonStatTable` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `rarity`
      if (jsonObj.get("rarity") != null && !jsonObj.get("rarity").isJsonNull()) {
        Rarity.validateJsonElement(jsonObj.get("rarity"));
      }
      if ((jsonObj.get("statTable") != null && !jsonObj.get("statTable").isJsonNull()) && !jsonObj.get("statTable").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `statTable` to be a primitive type in the JSON string but got `%s`", jsonObj.get("statTable").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SummonStatTable.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SummonStatTable' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SummonStatTable> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SummonStatTable.class));

       return (TypeAdapter<T>) new TypeAdapter<SummonStatTable>() {
           @Override
           public void write(JsonWriter out, SummonStatTable value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SummonStatTable read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SummonStatTable given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SummonStatTable
   * @throws IOException if the JSON string is invalid with respect to SummonStatTable
   */
  public static SummonStatTable fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SummonStatTable.class);
  }

  /**
   * Convert an instance of SummonStatTable to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

