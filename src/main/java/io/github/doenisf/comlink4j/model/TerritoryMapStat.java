/*
 * swgoh-comlink
 * An API for sending requests to Star Wars Galaxy of Heroes' game services
 *
 * The version of the OpenAPI document: 0.34.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.github.doenisf.comlink4j.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.doenisf.comlink4j.model.TerritoryPlayerStat;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.github.doenisf.comlink4j.JSON;

/**
 * TerritoryMapStat
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-10-05T21:53:04.897531600+02:00[Europe/Berlin]", comments = "Generator version: 7.8.0")
public class TerritoryMapStat {
  public static final String SERIALIZED_NAME_MAP_STAT_ID = "mapStatId";
  @SerializedName(SERIALIZED_NAME_MAP_STAT_ID)
  private String mapStatId;

  public static final String SERIALIZED_NAME_PLAYER_STAT = "playerStat";
  @SerializedName(SERIALIZED_NAME_PLAYER_STAT)
  private List<TerritoryPlayerStat> playerStat = new ArrayList<>();

  public TerritoryMapStat() {
  }

  public TerritoryMapStat mapStatId(String mapStatId) {
    this.mapStatId = mapStatId;
    return this;
  }

  /**
   * Get mapStatId
   * @return mapStatId
   */
  @javax.annotation.Nullable
  public String getMapStatId() {
    return mapStatId;
  }

  public void setMapStatId(String mapStatId) {
    this.mapStatId = mapStatId;
  }


  public TerritoryMapStat playerStat(List<TerritoryPlayerStat> playerStat) {
    this.playerStat = playerStat;
    return this;
  }

  public TerritoryMapStat addPlayerStatItem(TerritoryPlayerStat playerStatItem) {
    if (this.playerStat == null) {
      this.playerStat = new ArrayList<>();
    }
    this.playerStat.add(playerStatItem);
    return this;
  }

  /**
   * Get playerStat
   * @return playerStat
   */
  @javax.annotation.Nullable
  public List<TerritoryPlayerStat> getPlayerStat() {
    return playerStat;
  }

  public void setPlayerStat(List<TerritoryPlayerStat> playerStat) {
    this.playerStat = playerStat;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TerritoryMapStat territoryMapStat = (TerritoryMapStat) o;
    return Objects.equals(this.mapStatId, territoryMapStat.mapStatId) &&
        Objects.equals(this.playerStat, territoryMapStat.playerStat);
  }

  @Override
  public int hashCode() {
    return Objects.hash(mapStatId, playerStat);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TerritoryMapStat {\n");
    sb.append("    mapStatId: ").append(toIndentedString(mapStatId)).append("\n");
    sb.append("    playerStat: ").append(toIndentedString(playerStat)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("mapStatId");
    openapiFields.add("playerStat");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to TerritoryMapStat
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TerritoryMapStat.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TerritoryMapStat is not found in the empty JSON string", TerritoryMapStat.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!TerritoryMapStat.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `TerritoryMapStat` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("mapStatId") != null && !jsonObj.get("mapStatId").isJsonNull()) && !jsonObj.get("mapStatId").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `mapStatId` to be a primitive type in the JSON string but got `%s`", jsonObj.get("mapStatId").toString()));
      }
      if (jsonObj.get("playerStat") != null && !jsonObj.get("playerStat").isJsonNull()) {
        JsonArray jsonArrayplayerStat = jsonObj.getAsJsonArray("playerStat");
        if (jsonArrayplayerStat != null) {
          // ensure the json data is an array
          if (!jsonObj.get("playerStat").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `playerStat` to be an array in the JSON string but got `%s`", jsonObj.get("playerStat").toString()));
          }

          // validate the optional field `playerStat` (array)
          for (int i = 0; i < jsonArrayplayerStat.size(); i++) {
            TerritoryPlayerStat.validateJsonElement(jsonArrayplayerStat.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TerritoryMapStat.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TerritoryMapStat' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TerritoryMapStat> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TerritoryMapStat.class));

       return (TypeAdapter<T>) new TypeAdapter<TerritoryMapStat>() {
           @Override
           public void write(JsonWriter out, TerritoryMapStat value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TerritoryMapStat read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of TerritoryMapStat given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of TerritoryMapStat
   * @throws IOException if the JSON string is invalid with respect to TerritoryMapStat
   */
  public static TerritoryMapStat fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TerritoryMapStat.class);
  }

  /**
   * Convert an instance of TerritoryMapStat to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

